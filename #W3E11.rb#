def odd_even(number)
  number % 2 == 0 ? "even" : "odd"
end

collected_inputs =  [12, 23, 456, 123, 4579]
collected_inputs.each do |num|
  puts "The number is #{num} \n"
  puts "The number is #{odd_even(num)} \n"
end
